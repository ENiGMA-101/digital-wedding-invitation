# ── Database (used by the RSVP endpoint) ────────────────────────
# Local / Vercel Postgres connection string
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5432/app_db"

# ── Site metadata (used for Open Graph absolute URLs) ──────────
NEXT_PUBLIC_SITE_URL="https://your-invitation.vercel.app"
